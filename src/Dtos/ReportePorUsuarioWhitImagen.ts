export class ReportePorUsuarioWhitImagen {
  id_report?: string;
  titulo?: string;
  categoria?: string;
  estado?: string;
  privacidadTexto?: string;
  fechaFormateada?: string;
  descripcion?: string;
  imagenUrl?: string; // ruta relativa que puedes concatenar con la base URL si quieres


  votos?: number;
}
